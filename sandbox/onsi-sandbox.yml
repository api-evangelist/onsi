generated: '2026-07-20'
method: searched
source: https://docs.onsi.com/api-reference/introduction
environments:
  - name: production
    base_url: https://api.onsi.com
  - name: sandbox
    base_url: https://api.onsi.dev
separation:
  mechanism: separate host per environment
  auth: >-
    A distinct x-api-key is issued per environment; request keys from the Onsi
    team. There is no key-prefix mode switch — the base URL selects the
    environment.
test_values:
  published: false
  notes: >-
    Onsi documents a dedicated sandbox host (api.onsi.dev) but does not publish
    magic test identifiers, test bank accounts, or test cards in the public
    docs. Test data is provisioned with the sandbox API key. No values are
    invented here.
